import { StyleSheet } from "react-native"

const style = StyleSheet.create({
    input: {
        borderWidth: 1,
        paddingStart: 15,
        borderColor: '#E0E0E0',
        borderRadius: 5
    }
})

export default style